export class ProductCategory {
    id: number;
    categoryName: string;

    constructor(rid: number, rcategoryName: string) {
        this.id = rid
        this.categoryName = rcategoryName
    }
    
}